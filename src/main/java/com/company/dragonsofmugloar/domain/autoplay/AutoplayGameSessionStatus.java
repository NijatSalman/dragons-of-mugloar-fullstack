package com.company.dragonsofmugloar.domain.autoplay;

/** Where an autoplay session stands: finished once every game is finished or failed. */
public enum AutoplayGameSessionStatus {
    RUNNING, FINISHED
}
